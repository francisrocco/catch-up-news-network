<%= form_for(post) do |f| %>
<%= f.label :title %>
<%= f.text_area :title %><br>
<%= f.label :link %>
<%= f.text_area :link %><br>
<%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name %>
<%= f.submit %>
<% end %>
