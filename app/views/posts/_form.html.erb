<%= form_for(@post) do |f| %>
  <%= f.text_area :title, placeholder: "Title" %><br>
  <%= f.text_area :link, placeholder: "Link" %><br>
  <%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name %><br>
<%= f.submit %>
<% end %>
