
<style>
.thumbnail{
    position: relative;
    width: 450px;
    height: 330px;
    overflow: auto;
    background: lightgray;
    border: 1px solid black;
    padding: 0;
}

a {
    color:black;
    text-decoration:none;
}

.image{
  position: absolute;
  width: 450px;
  max-height:235px;
  overflow: hidden;
}

.headline{
    position: absolute;
    opacity: 0.55;
    bottom:0;
    background: white;
    width: 100%;
    padding: 10px;
}

.bottom-portion{
  position: absolute;
  bottom:0px;
  padding: 5px 10px;
  width: 450px;
  max-height:95px;
}

.caption {
  position: absolute;
  bottom: 35px;
}

.button {
    background-color: #b3b3cc;
    border: none;
    color: white;
    padding: 1% 3%;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 12px;
    margin: 4px 2px;
    cursor: pointer;
    border-radius: 32px;
}

</style>
<div class='thumbnail'>
  <a target="_blank" href="<%= post.link %>">
  <div class='image'>
    <img src="<%= post.image %>" width="450">
      <div class='headline'>
          <div class='short' color='black'><h3><%= post.post_title %></h3></div>
    </div>
  </div>
  <div class="caption">
    <%= post.description %><br>
  </div>
<div class='vote-arrows pull-left bottom-portion'>
    <span id= "voteUp">
      <%= link_to " ", post_up_vote_path(post), class: 'glyphicon glyphicon-thumbs-up', method: :post, remote: true %>
    </span>
    &nbsp
    <span id='post_<%=post.id%>_votes'>
    <strong><%= post.total_votes %></strong>
    </span>
    &nbsp
    <span id= "myToolTip" >
      <%= link_to " ", post_down_vote_path(post), class: "glyphicon glyphicon-thumbs-down", method: :post, remote: true   %>
    </span>
    &nbsp
    <% post.tags.each do |tag| %>
      <a href="/tags/<%= "#{tag.id}" %>"><button class="button"><%= "#{tag.name}" %></button></a>
    <% end %>
  </div>

  </a>
</div> 