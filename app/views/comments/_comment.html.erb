<% if post.comments.size > 0 %>
	<h3>Comments</h3>
	<ul id="Comments">
	<% post.comments.each do |comment| %>
		<li><strong><%= comment.post.user.name %> wrote: </strong><%= comment.message %></li>
	<% end %>
	</ul>
<% else %>
	<h3>No comments yet. Start a conversation!</h3>
<% end %>
